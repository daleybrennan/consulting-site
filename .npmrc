# Production PDF rendering uses puppeteer-core + @sparticuz/chromium (see
# src/lib/render-pdf.ts). The full `puppeteer` dev dependency would otherwise
# download ~150MB of Chromium during install — skip it to keep CI/Vercel builds
# fast and within serverless function size limits.
puppeteer_skip_download=true
